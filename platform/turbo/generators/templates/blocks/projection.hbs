/**
 * {{ pascalCase name }}Projection — Projection skeleton (`<Name>Projection`).
 *
 * TODO(Phase 1): implement per R2 F2.9 P3 (a projection never becomes a fact)
 * and F3.1 (deterministic, recalculable derivation of named facts; never
 * persisted as truth; never the source of a refusal).
 */
export class {{ pascalCase name }}Projection {
  // Intentionally empty at Phase 0: skeleton only.
}
