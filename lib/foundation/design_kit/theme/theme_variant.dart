/// The official theme variants — value-set identifiers under which
/// every themed Token declines (DTV-03). Mentora Emerald is the
/// official accent; the historical blue no longer exists. Future
/// accents are additional value sets under the same names — never a
/// Design System change (GE §5).
enum ThemeVariantId { light, dark, lightHighContrast, darkHighContrast }
