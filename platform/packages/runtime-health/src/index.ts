/**
 * @mentora/runtime-health — the runtime's health verdicts (F5.1 R-6:
 * Readiness = the three Sequences, Liveness = the process, neither ever
 * judges business; fail closed).
 */

export * from './health.js';
