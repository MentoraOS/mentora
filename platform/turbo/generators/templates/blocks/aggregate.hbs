/**
 * {{ pascalCase name }} — Aggregate skeleton.
 *
 * TODO(Phase 1): implement per R2 source/domain/01-tactical-building-blocks.md
 * (Aggregate: the executable incarnation of the NO-holder; smallest unit that
 * keeps its invariant true) and the frozen design of its bounded context.
 * NO invariant, command or event may be invented here — they exist only in R2.
 */
export class {{ pascalCase name }} {
  // Intentionally empty at Phase 0: skeleton only.
}
