/**
 * {{ pascalCase name }}Adapter — Adapter skeleton (`<Provider><Capability>Adapter`).
 *
 * TODO(Phase 1): implement per R2 F4.4 I-4 (an adapter serves ONE frontier,
 * creates no fact, no Decision, no truth; vendor types die here). The only ring
 * allowed to import the vendor SDK (ADR-0003, family Adapters).
 */
export class {{ pascalCase name }}Adapter {
  // Intentionally empty at Phase 0: skeleton only.
}
