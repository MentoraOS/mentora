/**
 * {{ pascalCase name }}Policy — Policy skeleton (`<Truth><Rule>Policy`).
 *
 * TODO(Phase 1): implement per R2 source/constitution/04-bilingual-dictionary.md
 * §6 and source/domain/01-tactical-building-blocks.md (Policy Objects: a rule
 * published in advance, parameters are product configuration, renders a
 * motivated Decision). The rule itself lives in R2 — never invented here.
 */
export class {{ pascalCase name }}Policy {
  // Intentionally empty at Phase 0: skeleton only.
}
