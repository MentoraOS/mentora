{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Root TypeScript SOLUTION file. It owns no sources (files: []); it lists the workspace packages' BUILD projects as references so `tsc -b` builds the whole graph in dependency order. Each package also has a tsconfig.json (IDE/lint/typecheck, tests included) and a tsconfig.build.json (emit, source only).",
  "files": [],
  "references": [
    { "path": "./packages/kernel/tsconfig.build.json" },
    { "path": "./packages/shared/tsconfig.build.json" },
    { "path": "./packages/contracts/tsconfig.build.json" }
  ]
}
