import { describe, expect, it } from 'vitest';

import { {{ pascalCase name }}Adapter } from './{{ kebabCase name }}.adapter.js';

describe('{{ pascalCase name }}Adapter', () => {
  it('exists (integration proves the port contract — F4.4 I-10)', () => {
    expect(new {{ pascalCase name }}Adapter()).toBeInstanceOf({{ pascalCase name }}Adapter);
  });
});
