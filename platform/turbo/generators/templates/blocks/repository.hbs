/**
 * {{ pascalCase name }}Repository — Repository PORT skeleton (`<Truth>Repository`).
 *
 * TODO(Phase 1): declare per R2 F3.1 (the registry of ONE truth: retain,
 * find by Identifier, the declared R-A key walks — never business logic) and
 * F5.2 S-1 (persistence keeps, never owns). This is the PORT (interface owned
 * by the domain); implementations live in adapters.
 */
export interface {{ pascalCase name }}Repository {
  // Intentionally empty at Phase 0: skeleton only.
}
